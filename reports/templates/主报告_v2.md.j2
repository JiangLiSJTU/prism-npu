# NPU 架构设计综合报告 v2.0

**版本**：v2.0（Phase F 真实 Timeloop 校准 + wall-clock 完整模型版）  
**日期**：2026-05-06  
**替代**：`NPU 架构设计_910B4_综合报告.md`（Phase A–E 版本）  
**覆盖阶段**：Phase A（实卡测试）→ B（msprof）→ C（Roofline v2）→ F（真实 Timeloop + wall-clock v3 + 310P 外推）

---

## 0. 执行摘要

本报告综合 910B4 msprof 真机测量与真实 Timeloop-mapper 校准，给出固定网络 AI 定制芯片设计的量化决策依据。**三条核心结论**：

### 结论 1：BERT 类工作负载的瓶颈是 host CANN runtime，硬件算力改动无效

| 模型 | wall-clock | T_npu | T_host | host 占比 |
|------|-----------|------|--------|---------|
| BERT-base b1（910B4）| {{ wall_clock_bert_ms }} ms | {{ T_npu_bert_ms }} ms | **{{ T_host_bert_ms }} ms** | **88.7%** |
| GPT-2-small b1（910B4）| {{ wall_clock_gpt2_ms }} ms | {{ T_npu_gpt2_ms }} ms | **{{ T_host_gpt2_ms }} ms** | **74.6%** |
| Qwen3-0.6B b1（910B4）| {{ wall_clock_qwen3_ms }} ms | {{ T_npu_qwen3_ms }} ms | **{{ T_host_qwen3_ms }} ms** | **79.0%** |

即使 NPU compute time 降至 0，BERT-base wall-clock 最优也只有 **{{ T_host_bert_ms }} ms**。Cube-MAC × 4 对 BERT wall-clock 的改善上限仅 **7.7%**。

### 结论 2：310P 上 GPT-2/Qwen3 进入 NPU 主导区间，硬件改动有效

310P 算力仅为 910B4 的 1/4（70 vs 280 TFLOPS），NPU compute time 放大 4×：

| 模型 | 310P NPU 估算 | host overhead | 310P 瓶颈 |
|------|-------------|-------------|---------|
| BERT-base L=12 | ~7.3 ms | ~14 ms | **host 主导** |
| GPT-2-small L=12 | **~18 ms** | ~14 ms | **NPU 主导** |
| Qwen3-0.6B L=28 | **~55 ms** | ~60 ms | **NPU 主导** |

GPT-2 和 Qwen3 在 310P 上 NPU compute 超过或接近 host overhead，架构改动（L2、Cube-MAC）有**真实影响**。

### 结论 3：310P 定制最高 ROI = L2 8→32 MB + CANN runtime 优化

> ⚠️ **置信度声明**：本结论中的所有 310P 数字均为基于 910B4 实测的**外推估计**（无 310P 真机 msprof 数据）。GPT-2 wall-clock 估算被 §7 自评为 **"低"** 置信度。**作出芯片投资决定前需 310P 真机验证**，详见 §7.1。

| 优化路径 | BERT wall-clock 改善 | GPT-2 wall-clock 改善 | ROI 评级（外推） |
|---------|---------------------|----------------------|---------|
| CANN runtime 优化（host）| **~30-90%** | ~40-50% | ⭐⭐⭐⭐⭐ |
| L2 8→32 MB | 0%（NPU 不是瓶颈）| **15-25%**（attention 命中）| ⭐⭐⭐⭐ ⚠️ 待验证 |
| Cube-MAC × 4 | ~0%（host 锁死）| **~22%**（NPU→14 ms）| ⭐⭐（GPT-2 场景）⚠️ 待验证 |
| HBM/DDR BW × 1.5 | 0%（实测 BW 利用率 0.3%）| 0% | ⭐ |

---

## 一、方法论演进：从估算到双重验证

### 1.1 各 Phase 核心修正

| Phase | 方法 | 关键修正 | 结论可信度 |
|-------|------|---------|-----------|
| A | ais_bench 延迟测量 | 发现 `latency_ms` = NPU op time，非 wall-clock | 高（硬件测量）|
| B | msprof 物理分解 | 揭示 host CANN overhead 14-60 ms 主导 wall-clock | 高（Profiler 直接测量）|
| C | Roofline v2 | β_layer 五点拟合 MAE=3.2%，encoder/decoder 双 β | 中（拟合模型）|
| D | Phase D Python "Timeloop" | **伪 Timeloop**：实为 Roofline v2 Python 封装；未调用真实 mapper | 低（已作废）|
| F1 | 真实 timeloop-mapper × BERT-base 8 GEMM | k_calib_910b4 = {{ k_calib_910b4 }}×，根因：mapper 仅用 3/24 核（9.38% 利用率）| 高（直接测量）|
| F3 | wall-clock 三项模型 | T_wall = T_host + T_npu；wall-clock MAE=0.57% | 高（msprof 实测）|
| F5 | 310P Timeloop 外推 | 310P cycles ≈ 910B4 cycles（差 <1%），仅时钟比 1.5× 影响延迟 | 中（部分 Timeout）|

### 1.2 Phase D 的历史教训

Phase D 脚本 `timeloop_sweep.py` 命名为"Timeloop 扫描"，但本质是调用 Roofline v2 解析公式的 Python 脚本——未调用任何 `timeloop-mapper` 二进制。该脚本无法发现：
1. mapper 的 spatial 映射选择（核利用率问题）
2. 真实的 tiling 质量与存储层次交互
3. Cube-MAC 密度改变时 cycle 数的实际变化

Phase F 用真实 Docker 化 timeloop-mapper 替代了 Phase D 的所有分析。**Phase D 的微架构探索结论已全部替换为 Phase F 的真实数据。**

---

## 二、Phase B 实测：双层瓶颈分解

### 2.1 wall-clock 拆解

| 模型 | ais_bench latency | wall-clock (msprof) | T_npu (msprof) | T_host (msprof) |
|------|-----------------|--------------------|--------------|-----------------|
| BERT-base b1 | 1.78 ms | **{{ wall_clock_bert_ms }} ms** | {{ T_npu_bert_ms }} ms | **{{ T_host_bert_ms }} ms** |
| BERT-base b16 | {{ T_npu_bert_ms }} ms（均值）| — | — | — |
| GPT-2-small b1 | {{ T_npu_gpt2_ms }} ms | **{{ wall_clock_gpt2_ms }} ms** | {{ T_npu_gpt2_ms }} ms | **{{ T_host_gpt2_ms }} ms** |
| Qwen3-0.6B b1 | {{ T_npu_qwen3_ms }} ms | **{{ wall_clock_qwen3_ms }} ms** | {{ T_npu_qwen3_ms }} ms | **{{ T_host_qwen3_ms }} ms** |

> **关键发现**：ais_bench `latency_ms` ≈ T_npu（NPU op time），而非 wall-clock。Phase A 数据有效，但测量的是 NPU compute time。

### 2.2 host overhead 双组分分析

```
T_host ≈ β_host_const + α × params_M

BERT-base(110M): 0.095×110 + 3.6 = 14.05 ms  实测={{ T_host_bert_ms }} ms  误差=2.3%
GPT-2(124M):     0.095×124 + 3.6 = 15.38 ms  实测={{ T_host_gpt2_ms }} ms  误差=19.3%（decoder 路径不同）
Qwen3(596M):     0.095×596 + 3.6 = 60.22 ms  实测={{ T_host_qwen3_ms }} ms  误差=0.9%
```

GPT-2 decoder-only 模型的 CANN dispatch 路径不同于 BERT encoder，host overhead 略低。

### 2.3 NPU 内部 MTE2 主导

| 模型 | aicore_time | MAC 占 | MTE2 占（数据搬运）| msprof Roofline 判定 |
|------|------------|--------|-------------------|---------------------|
| BERT-base | 558 μs | 29.5% | **79.4%** | memory-MTE2 bound |
| GPT-2-small | 1,373 μs | 43.5% | **67.2%** | memory-MTE2 bound |
| Qwen3-0.6B | 5,533 μs | 55.7% | **74.8%** | memory-MTE2 bound |

MTE2（GM↔L1 数据搬运管线）是 NPU 内部真正瓶颈。扩大 L2 可减少 MTE2 stall，但只影响 T_npu 部分（≤11-25% wall-clock）。

### 2.4 HBM 利用率实测

HBM 实测 BW = 1.3–1.7 GB/s（仅为 392 GB/s 的 **0.3–0.4%**）。权重预加载到 NPU 私有内存段，inference 时几乎不流式访问 HBM。因此：
- DDR/HBM BW 扩容对 inference 延迟 **零贡献**（0.3% 利用率）
- 310P 的 204.8 GB/s DDR 在 BERT/GPT-2 工作负载下不是瓶颈

---

## 三、Roofline v3 wall-clock 模型

### 3.1 三层次 Roofline 公式

```
层次 1（wall-clock，用户体验）：
  T_wall = T_host + T_npu
  T_host = f(参数量, CANN 版本) ≈ 13–60 ms
  T_npu  = β_layer × L（msprof 直接测量）

层次 2（NPU compute，硬件评估）：
  T_npu = max(T_compute, T_memory)
  T_compute = FLOPs / (TFLOPS × η_compute)
  制度：910B4 所有测试模型均 compute-bound on NPU（HBM 不是 NPU 瓶颈）

层次 3（Timeloop 映射质量，架构设计用）：
  T_timeloop / T_npu_real ≈ k_calib
  k_calib_910b4 = {{ k_calib_910b4 }}×（mapper 仅用 3/24 核）
  k_calib_310p  ≈ {{ k_calib_310p }}×（310P cycles ≈ 910B4 cycles，仅时钟 1.5× 差）
```

### 3.2 v3 校准常数（权威值）

```python
# 910B4 v3 校准常数（2026-05-06，Phase F 真机验证）
ASCEND_910B4_V3 = {
    "FP16_TFLOPS": 280.0,
    "HBM_BW_GBs":  392.0,
    "L2_MB":        96.0,    # 权威值：100663296 B ÷ 1048576
    "cores":        24,
    "clock_GHz":    1.5,
    "TDP_W":        80,
}

CALIB_V3 = {
    # NPU compute（msprof Cube 直接测量）
    "beta_layer_enc_d768_us":      129.3,   # BERT-base，encoder，S=128
    "beta_layer_dec_S512_d768_us": 366.0,   # GPT-2-small，decoder，S=512
    "beta_layer_dec_S512_d1024_us": 565.0,  # Qwen3-0.6B，decoder，S=512，d=1024
    # wall-clock host overhead（msprof step_trace 直接测量）
    "T_host_BERT_ms":  {{ T_host_bert_ms }},
    "T_host_GPT2_ms":  {{ T_host_gpt2_ms }},
    "T_host_Qwen3_ms": {{ T_host_qwen3_ms }},
    # Timeloop 修正系数（真实 mapper 校准）
    "k_calib_910b4":     {{ k_calib_910b4 }},
    "k_calib_310p_est":  {{ k_calib_310p }},
}

ASCEND_310P_V3 = {
    "FP16_TFLOPS":   70.0,
    "DDR_BW_GBs":    204.8,
    "L2_MB":          8.0,    # 8核共享
    "cores":          8,
    "clock_GHz":      1.0,
    "TDP_W":          67,
    "compute_ratio":  0.25,   # 70/280 = 1/4
    "clock_ratio":    0.667,  # 1.0/1.5
    "TL_cycle_ratio": 1.0,    # Timeloop cycles ≈ 同（mapper 独立于核数）
    "TL_latency_ratio": 1.5,  # 仅时钟频率差异
}
```

### 3.3 wall-clock 预测精度

| 模型 | T_wall 预测 | T_wall 实测 | MAE |
|------|-----------|-----------|-----|
| BERT-base b1 | {{ T_host_bert_ms }} + 1.55 = **15.93 ms** | {{ wall_clock_bert_ms }} ms | 1.7% |
| GPT-2-small b1 | {{ T_host_gpt2_ms }} + {{ T_npu_gpt2_ms }} = **{{ wall_clock_gpt2_ms }} ms** | {{ wall_clock_gpt2_ms }} ms | 0.06% |
| Qwen3-0.6B b1 | {{ T_host_qwen3_ms }} + {{ T_npu_qwen3_ms }} = **{{ wall_clock_qwen3_ms }} ms** | {{ wall_clock_qwen3_ms }} ms | 0.05% |
| **平均** | — | — | **0.57%** |

---

## 四、Phase F Timeloop 真实校准

### 4.1 910B4 BERT-base F1 校准结果

| 算子 | 维度（M×N×K）| Timeloop 周期 | 核利用率 | 延迟（@1.5GHz，μs）|
|------|------------|-------------|---------|------------------|
| q_projection | 128×768×768 | 8,192 | 9.38% | 5.46 |
| k_projection | 128×768×768 | 8,192 | 9.38% | 5.46 |
| v_projection | 128×768×768 | 8,192 | 9.38% | 5.46 |
| attention_qkt | 128×128×64（H=12）| 786,432 | 0.02% | 524.3 |
| attention_av | 128×64×128（H=12）| 786,432 | 0.02% | 524.3 |
| output_projection | 128×768×768 | 8,192 | 9.38% | 5.46 |
| ffn_linear1 | 128×3072×768 | 25,165,824 | 0.01% | 16,777 |
| ffn_linear2 | 128×768×3072 | 393,216 | 0.78% | 262.1 |
| **1层合计** | | **26,954,240** | 加权均值 0.12% | **18,110** |

**根因**：mapper 在 L2_Buffer spatial 维度固定选择 N=3 核（跨 subtree spatial 约束在 classic mapper 中不生效），导致核利用率仅 9.38%。修正系数：

```
k_calib_910b4 = 18,110 μs / 129.3 μs = {{ k_calib_910b4 }}×
```

### 4.2 310P F1 Timeloop 校准结果

| 算子 | 310P Cycles | 910B4 Cycles | 比值 | 状态 |
|------|------------|-------------|------|------|
| q_projection | 8,192 | 8,192 | **1.0×** | ✓ |
| k_projection | 8,192（估）| 8,192 | ~1.0× | Timeout |
| v_projection | 8,192（估）| 8,192 | ~1.0× | Timeout |
| attention_qkt | 786,432 | 786,432 | **1.0×** | ✓ |
| attention_av | 786,432 | 786,432 | **1.0×** | ✓ |
| output_projection | 8,192（估）| 8,192 | ~1.0× | Timeout |
| ffn_linear1 | 25,165,824 | 25,165,824 | **1.0×** | ✓ |
| ffn_linear2 | 393,216（估）| 393,216 | ~1.0× | parse 失败 |
| **1层合计** | **~27,165,000** | **26,954,240** | **~1.008×** | 部分 Timeout |

**关键发现**：310P Timeloop cycles ≈ 910B4 Timeloop cycles（差异 <1%）。

```
T_310p_us = 27,165,000 / 1.0e9 × 1e6 = 27,165 μs
T_910b4_us = 26,954,240 / 1.5e9 × 1e6 = 17,969 μs

比值：27,165 / 17,969 ≈ 1.512×  = 时钟频率比（1.5 GHz / 1.0 GHz）✓
```

310P 延迟差异**完全由时钟频率决定**，与核数/L2/BW 无关（mapper 使用相同的绝对核数 N=3）。

### 4.3 Cube-MAC Pareto 分析

**F4 实测重要发现**：Timeloop classic mapper 在 MAC 数组大小变化时无法找到优质 tiling。focused sweep（`run_cm_sweep_focused.py`）完整结果：

| CM 倍率 | 总 cycles（authoritative JSON）| ratio vs 基线 | 关键算子 ffn_linear1 |
|---------|-------------------------------|--------------|---------------------|
| 1.0×（基线）| 26,954,240 | 1.0× | 25,165,824 |
| 2.0× | 40,550,400 | **1.504×（差 50%）**| 37,748,736 |
| 4.0× | 40,132,608 | **1.489×（差 49%）**| 37,748,736（与 cm=2.0 完全相同 → tiling floor）|

**根因**：随机搜索预算（victory-condition=500）在新 MAC 粒度下无法收敛；ffn_linear1 在 cm=2.0/4.0 下都收敛到同一个 37,748,736 周期"地板"，证明这是 mapper 限制而非物理限制。MAC scale 分析必须改用物理模型（T_npu ∝ FLOPs / throughput）：

| Cube-MAC 倍率 | 方法 | 910B4 NPU cycles | T_npu（μs）| T_wall BERT b1 | Δwall-clock |
|-------------|------|-----------------|-----------|----------------|-------------|
| 0.5× | 物理模型 | ~53.9M | ~258 | 14.66 ms | +1.7% |
| **1.0×（基线）** | **msprof 实测** | **26,954,240** | **129.3** | **15.95 ms** | **0%** |
| 2.0× | 物理模型 | ~13.5M | ~65 | 14.65 ms | -8.2% |
| 4.0× | 物理模型 | ~6.7M | ~32 | 14.51 ms | -9.0% |

> **结论**：Cube-MAC × 4 对 BERT-base wall-clock 改善上限约 9%（从 15.95 ms → 14.51 ms），几乎全部被 {{ T_host_bert_ms }} ms 的 host overhead 所锁定。  
> Timeloop 在 MAC scale 场景不可靠（mapper 搜索失效），但在跨芯片比较（310P vs 910B4 等价性）场景中可信。

**Timeloop 有效 vs 无效分析维度总结**：

| 维度 | Timeloop 可信度 | 根因 |
|------|---------------|------|
| `cube_mac_scale` 变化 | ❌ mapper 失效 | 搜索预算不足，新粒度下找不到优质 tiling |
| `l2_scale` 变化 | ❌ 零效果 | DRAM 无限带宽建模；mapper 不优先 L2 常驻 |
| `bw_scale` 变化 | ❌ 零效果 | Timeloop classic 不建模 DRAM bandwidth |
| 跨芯片比较（310P vs 910B4）| ✅ 可信 | cycles 等价性揭示时钟比主导 |
| 单配置 k_calib 修正系数 | ✅ 可信（cm=1.0）| msprof 实测对照验证 |

---

## 五、双芯片对比分析

### 5.1 910B4 vs 310P 关键参数

| 参数 | 910B4 | 310P（Atlas 300I Pro）| 比值 |
|------|-------|---------------------|------|
| FP16 算力（整卡）| 280 TFLOPS | 70 TFLOPS | **4.0×** |
| AI Core 数量 | 24 | 8 | 3.0× |
| 时钟频率 | 1.5 GHz | 1.0 GHz | 1.5× |
| 内存带宽 | 392 GB/s（HBM2e）| 204.8 GB/s（LPDDR4X）| 1.91× |
| L2 缓存（共享）| 96 MB | **8 MB** | **12×** |
| L1/核 | 512 KB | 1 MB | 0.5×（310P 更大）|
| TDP | ~80 W | ~67 W | 1.2× |
| TOPS/W（FP16）| **3.5** | 1.04 | **3.4×** 差距 |

### 5.2 各模型 wall-clock 对比

| 模型 | 910B4（实测）| 310P（外推）| 说明 |
|------|-----------|-----------|------|
| BERT-base b1 | **14.4 ms** | **~14.4 ms** | 两芯片均 host 主导，硬件贡献 <11% |
| GPT-2-small b1 | ~17.3 ms | **~32 ms** | 310P NPU 主导；Cube-MAC 有效 |
| Qwen3-0.6B b1 | ~75.5 ms | **~69 ms** | 310P host 同等；NPU 接近临界 |

> **关键推论**：fixed network 主力工作负载（BERT 类）在两款芯片上均受 host CANN overhead 锁定，算力差异（4×）对用户体验延迟的贡献不到 11%。

### 5.3 瓶颈制度分类

| 场景 | 瓶颈 | 优化路径 |
|------|------|---------|
| BERT on 910B4 | host CANN（88.7%）| CANN runtime 优化 |
| BERT on 310P | host CANN（~69%）| CANN runtime 优化（同）|
| GPT-2 on 910B4 | host CANN（75%）| CANN runtime 优化 |
| **GPT-2 on 310P** | **NPU compute（~56%）** | **Cube-MAC + L2 扩大** |
| **Qwen3 on 310P** | **NPU compute（~79%）** | **Cube-MAC（BW 不是瓶颈）** |

---

## 六、行动建议

### 6.1 软件优先（最高 ROI，0 芯片面积成本）

**优先级 1：CANN runtime 优化（ModelLoad 缓存 + async dispatch）**

- 目标：将 T_host 从 14 ms 降至 3–5 ms
- 改善：BERT wall-clock 从 16.2 ms → 5–7 ms（**改善 55–70%**）
- 适用：所有 BERT 类工作负载，两款芯片通用
- 具体方案：
  1. ModelLoad 结果缓存到 pinned memory（首次加载后 amortize）
  2. 算子 dispatch pipeline（host/device 异步重叠）
  3. HCCL off 路径优化（纯 inference 不需 HCCL 开销）

### 6.2 310P 定制硬件（面向 fixed network 工作负载 sweet spot）

> ⚠️ **本节所有 310P 数字为外推估计，置信度等级见 §7。L2/Cube-MAC 推荐评级为"待真机 msprof 验证"。** 推进硬件改动前，建议先完成 §8 列出的"310P 真机 msprof 实测"项。

| 参数 | 现有 310P | 推荐定制值 | ROI（外推） | 改善理由 |
|------|---------|-----------|-----|---------|
| L2 缓存 | **8 MB** | **32 MB** | ⭐⭐⭐⭐ ⚠️ 待验证 | GPT-2 attention（S²=512²，12头≈6MB）命中率提升；解锁 NPU 主导模型的 attention 带宽瓶颈 |
| CANN runtime | 当前 | **ModelLoad 缓存 + async dispatch** | ⭐⭐⭐⭐⭐ | BERT wall-clock 改善 ~30-70%（host 主导路径最高 ROI）|
| DDR 带宽 | 204.8 GB/s | **256 GB/s（+25%）** | ⭐⭐⭐ | Qwen3 类模型；成本增量低（BW 利用率仍低）|
| Cube-MAC/核 | 4096 | **4096（维持）** | ⭐ | BERT 场景 NPU < host，扩 MAC 无效 |
| AI Core 数 | 8 | **8–12（维持）** | ⭐ | 核数增加不改变 host 主导的 wall-clock |

**核心判断**：fixed network 主流工作负载（BERT 类）在 310P 上的 wall-clock 与 910B4 **相同**（~14 ms）——两者都受 host 锁定。310P 的 GPU 算力劣势（4×）对 BERT 用户体验**无实质影响**。定制 ROI 最高路径是 L2 扩容（解锁 GPT-2 NPU 主导工作负载）+ CANN 软件优化（解锁 BERT host 主导工作负载）。

### 6.3 避免的方向

| 方向 | 为什么避开 |
|------|----------|
| HBM/DDR BW 大幅扩容 | 实测 BW 利用率仅 0.3-0.4%，扩容 ROI = 0 |
| Cube-MAC 大幅扩大（>2×）| BERT 场景被 host 锁定；GPT-2 只需 2× 即可达临界 |
| 核数从 8 → 24 | Timeloop 证明核利用率已被 mapper 锁定在 N=3，增核无效 |
| AI 优化编译器（针对 NPU 算子效率）| NPU op 仅占 11-25% wall-clock，优化杠杆有限 |

### 6.4 Phase N pipe-aware sweep 修订（2026-05-09 新增）

Phase N 用 msprof PipeUtilization 实测把 β_layer 拆解为 7 类 pipe，发现之前 Phase J v2 因 wall_clock = `max(T_compute, β_layer)` 公式吞掉了 device-side pipe 信号。**v3 pipe-aware sweep 修正了多项关键结论**：

#### 6.4.1 新解锁的真实敏感维度（v2 错称 ratio=1.0）

| 维度 | 原 v2 ratio | **v3 ratio** | 物理依据 |
|------|----------:|-----------:|---------|
| HBM → LPDDR4X (50 GB/s)，BERT-base | 1.00 | **1.22** | aic_mte2 实测 79.4%，HBM 真敏感 |
| HBM → LPDDR4X，Qwen3-prefill-S4096 | — | **2.61** | aic_mte2 54% + aiv_mte2 73% 双瓶颈 |
| HBM → LPDDR4X，**Qwen3 decode** | — | **2.88** | aic_mac 仅 6.6%，HBM 是唯一瓶颈 |
| HBM → HBM3 (800)，Qwen3 decode | 1.00 | **0.86** | decode workload 仍有 BW headroom |
| **UB+L1 融合**，Qwen3-prefill-S4096 | — | **0.80** | aiv_mte2 73% 流量被融合内存池消除 |
| ~~FixPipe 减半，Qwen3-prefill-S4096~~ | — | ~~1.24~~ → **1.00** | **见下方修正** |
| AIV : AIC = 1 : 1（固定网络 workload）| 1.00 | 1.00 | aiv_vec 占 7-8%，AIV 减半零损失 |

> **修正（Issue #7，2026-05-21）：FixPipe 杠杆实为 HBM 带宽杠杆。**
> 早期 sweep 把 `aic_fixpipe` 整体按 FixPipe 单元带宽（`fixpipe_bw`）缩放，得出
> "FixPipe 减半 → Qwen3-prefill-S4096 ratio 1.24"。**Prior-based 2-cluster 校准**
> （逐 op 按 implied_bw 阈值 `sqrt(hbm·onchip)` 分到 GM/片上簇，`gm_frac` = GM
> 簇字节占比；见 [05_calibration.md §3.2](../methodology/05_calibration.md)）发现：
> `aic_fixpipe` 的 store 主要是 **L0C→GM 直写**——Qwen3-prefill-S4096 的
> `gm_frac ≈ 0.98`（98% 字节走 GM）。
> 修正后 **FixPipe 减半 ratio 仅 1.00（几乎无效）**；该 workload 真正的杠杆是
> **HBM 带宽**（`hbm_bw=50` 时 ratio 升到 ~3.5）。
> `aic_fixpipe` 仍是 Qwen3-prefill-S4096 的 dominant aic pipe（长 prefill attention
> 输出回写），但"投资 FixPipe 单元带宽"应改为"投资 HBM 带宽"。这是昇腾访存
> 瓶颈的核心 —— 真瓶颈在 HBM，曾被模型误记成一个片上 knob。

→ 该结论解锁了**自研定制 AI 芯片立项的真实可行设计空间**——之前 v2 错称的"架构无关性"实际上仅在 host overhead 主导路径上成立。

#### 6.4.2 固定网络定制 AI 芯片 v3 推荐起点（"910B4-mini-v3"）

| 维度 | 推荐值 | 固定网络 wall-clock 影响 |
|------|------|-------------------|
| AICore 数 | 16 (vs 24) | 0% (BERT) |
| Cube spatial | 16×16×16（保留）| — |
| L2 大小 | 32 MB (vs 96) | 0% |
| **HBM** | **LPDDR5X 100 GB/s** (vs HBM2e 392) | BERT +9%、GPT-2 +19%、NetT +11% |
| AIV : AIC | **1 : 1** (vs 2:1) | 0% (BERT/NetT)，GPT-2 +7% |
| TDP | 200 W (vs 300) | 0% |
| **β_host_gap / kernel** | **CANN runtime 优化目标 < 10 μs** (vs 41.6) | **固定网络业务最大收益维度**（host gap 占 wall 87%）|

**关键修订**：v2 错称 LPDDR5X 替代 HBM 零代价；v3 pipe-aware 模型显示 +9-19% 真实代价。**只有 host gap 软件优化做完后**，LPDDR5X 才能算"零损失"。LLM-friendly 路径独立——见架构 sweep v3 报告。

#### 6.4.3 LLM serving 主战场补强（decode workload）

Phase N 首次实测 Qwen3 decode（S_in=4 + past_kv S_kv=128 + b=1）的 PipeUtilization：
- aic_mac 仅 **6.6%**（Cube 严重过剩）
- aic_mte2 **84.8%**（HBM 极度主导）
- LPDDR4X 退化至 ratio **2.88**（接近线性 BW 退化）

→ **LLM serving 决策**：保持 HBM2e 或升级 HBM3；Cube 算力可砍 50%；UB+L1 融合给 decode 7% 加速。

详见 `legacy/docs/architecture_sweep_report.md` v3、`legacy/docs/arch_hypothesis_rules.md`、`legacy/docs/overhead_decomposition_audit.md`。

#### 6.4.4 算子优化天花板分析（v3 新工具）

为回答"现有固定网络场景仅靠算子优化能否解决 vector 瓶颈？"，新增 5 情景天花板预测工具（`scripts/calibration/predict_optimization_ceiling.py`）。从 9 配置 PipeUtil 实测自动推导：

| Workload | S1 算子优化降幅 | S2 + CANN runtime | S3 + UB+L1 融合 | S4 + HBM3 |
|---------|--------------:|-----------------:|----------------:|----------:|
| BERT-base S=128 b=1 | 8.6% | **74.6%** | 75.2% | 76.9% |
| GPT-2 S=512 b=1 | 19.1% | **67.7%** | 67.7% | 70.0% |
| Net-Transformer S=256 | 3.0% | **87.1%** | 87.5% | 88.4% |
| Qwen3-prefill-S256 b=1 | **43.5%** | 43.5% | 51.8% | 62.6% |
| Qwen3-prefill-S4096 b=1 | **35.6%** | 35.6% | **56.1%** | 56.1% |
| Qwen3-decode | **59.5%** | 59.5% | 66.2% | **80.2%** |

**关键含义**（固定网络场景）：
- **固定网络业务**：S2 总降 67-87%，但 S1 单独只降 3-19%——**软件 vector 瓶颈不是 algorithmic，是 host_gap**；UB+L1 融合（S3）几乎无收益（< 1%）。结论：**仅靠算子优化（不动 host runtime + 不动硬件）解决 vector 瓶颈空间小**。
- **LLM 长上下文（S=4096）**：S1 已达 35.6%（接近 vector 瓶颈算子层天花板）；UB+L1 融合（S3）追加 **20.5%**（53.5%-33.0%）——**这是仅靠算子优化无法跨越的硬件 lever**。
- **LLM decode**：S1 软件降 60%（aiv_idle 大量可优化）+ S4 HBM3 再降 14% → 整体 80% 优化空间——**HBM3 是首要硬件投资**。

---

## 七、风险与置信度

| 分析环节 | 置信度 | 说明 |
|---------|--------|------|
| 910B4 wall-clock / T_npu / T_host | **高** | msprof step_trace + op_summary 直接测量 |
| 910B4 Timeloop k_calib=140× | **高** | 真实 mapper vs msprof 实测直接对比 |
| 310P Timeloop cycles ≈ 910B4 | **中-高** | 主要算子（ffn_linear1 = 93%）实测确认；部分投影算子 Timeout |
| 310P wall-clock BERT（~14 ms）| **中** | host overhead 假设与 910B4 相近；无 310P 真机 msprof |
| 310P wall-clock GPT-2（~32 ms）| **低** | 基于算力比外推，无 310P 实测 |
| 面积/功耗估算 | **很低** | 线性缩放简化模型，非 PPA 工具输出 |
| CANN runtime 优化效果（30-70%）| **低** | 基于 host overhead 物理上限，实际需 CANN 专家验证 |

### 7.1 310P 外推的系统性偏差风险

- **DDR vs HBM**：310P 使用 LPDDR4X（非 HBM），大模型推理时内存带宽表现会有差异
- **CANN 版本**：310P 可能运行不同版本 CANN，host overhead 可能不同（±50% 不确定性）
- **工艺节点**：310P DaVinci Lite 工艺与 910B4 不同，功耗/面积估算需单独 PPA

---

## 八、待完成工作

| 项目 | 优先级 | 说明 |
|------|--------|------|
| 310P 真机 msprof 实测 | **高** | 当前 310P 结论全部外推；需真机验证 host overhead 和 NPU compute |
| Cube-MAC sweep 真实输出 | — | ✅ 已完成（cm=2.0/4.0 均 ~1.5× cycles，mapper 失效；MAC scale 改用物理模型）|
| CANN runtime 优化实验 | **高** | ModelLoad 缓存等具体改动需 CANN 专家协作 |
| wall-clock 模型 B>1 验证 | 中 | 当前仅 b1 验证（3 点）|
| Accelergy 面积/功耗输出 | 低 | 需 310P 工艺节点确认后才有意义 |

---

## 附录 A：版本演进

| 版本 | 方法 | 核心指标 | 替代状态 |
|------|------|---------|---------|
| v1.0（Phase A–C）| ais_bench + Roofline v1 拟合 | β_layer=85μs/层，MAE=9.3% | 历史版本 |
| Phase D（Python "Timeloop"）| Roofline v2 公式封装 | **伪 Timeloop**，结论无效 | 已废弃 |
| `NPU 架构设计_910B4_综合报告.md`（Phase A–E）| msprof + Roofline v2 | NPU compute MAE=3.2%，wall-clock 未建模 | **被本文替代** |
| **本文（v2.0，Phase A–F）** | **msprof + 真实 Timeloop + wall-clock v3** | **wall-clock MAE=0.57%，Timeloop k=140×** | **当前权威版本** |

---

## 附录 B：关键文件索引

| 文件 | 内容 |
|------|------|
| `910B4_roofline_校准报告_v3.md` | v3 Roofline 详细校准（wall-clock 三项模型 + Timeloop 验证）|
| `微架构探索_报告_v3.md` | 真实 Timeloop 微架构探索报告（替代 v1.1 Python 模拟版）|
| `legacy/docs/timeloop_calibration_910b.md` | 910B4 F1 Timeloop 校准详情（根因分析）|
| `legacy/docs/microarch_310p_extrapolation.md` | 310P 外推详细数据（F1 校准表 + 修正系数）|
| `timeloop_sweep_v3.py` | 真实 Timeloop sweep 脚本（调用 Docker mapper）|
| `run_cm_sweep_focused.py` | 910B4 Cube-MAC 专项扫描（cm=2.0, 4.0，serial）|
| `arch/ascend_910b4.yaml` | 910B4 Timeloop 架构（权威参数）|
| `arch/ascend_310p_timeloop.yaml` | 310P Timeloop 架构（修正版，70 TFLOPS / 8核 / L2=8MB）|
| `roofline_910b4_calibrated_v2.py` | Roofline v2 校准脚本（7 模型，21 点）|
| `legacy/docs/cube_efficiency_calibration.md` | **Phase M+** Cube 真实利用率实卡校准（physics-informed η_real fit v5，BERT 验证 MAE 14.33 pp）|
| `legacy/docs/architecture_sweep_report.md` | **Phase J v3 (pipe-aware)** 12 维度架构 sweep（49 变体 × 11 模型，pipe-aware 公式）|
| `legacy/docs/overhead_decomposition_audit.md` | **Phase N** β_layer overhead 分解审计（9 配置 PipeUtil 实测 + 1 真 decode）|
| `legacy/docs/arch_hypothesis_rules.md` | **Phase N** pipe % 阈值 → arch 推荐规则表 |
| `docs/findings/optimization_ceiling.md` | 算子/软件/硬件 5 情景优化天花板分析（按 workload 给出 S1-S4 wall-clock 降幅，prism-ceiling 活动输出）|
| `data/optimization_ceiling.json` | **Phase N** 5 情景 × 11 配置完整数据（CANN TBE 算子优化天花板 + 硬件可叠加增益）|
| `scripts/calibration/predict_optimization_ceiling.py` | **Phase N** 优化天花板预测工具（M1 后将迁到 src/prism/ceiling/）|
| `data/eta_physics_fit.json` | η_real(M, N, K, B) physics-informed 拟合参数（Phase M+）|
| `data/pipe_baseline_per_model.json` | **Phase N** 9 配置 msprof PipeUtil 实测 baseline（按 model_key 索引）|
| `data/phase_j_sweep_v3.json` | **Phase J v3** 49 变体 sweep + verification（pipe-aware）|
| `arch/ascend_910b4_for_sweep_v2.yaml` | **Phase J v3** baseline arch（含 12 个细粒度字段：l1_l0_bw, fixpipe_bw, ub_l1_bw 等）|
| `scripts/phase_j/sweep_v3.py` | **Phase J v3** pipe-aware sweep 主脚本（替代 sweep.py）|
| `scripts/calibration/fit_eta_physics.py` | Physics-informed η_real fit 拟合脚本 v5 |
| `benchmark/export_qwen3_decode.py` | **Phase N** Qwen3 decode ONNX 导出（含 KV cache reload 路径）|
